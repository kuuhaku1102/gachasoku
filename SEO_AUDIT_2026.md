# gachasoku SEO監査レポート（2026年版）

## 📋 監査日時
2026年1月6日

## 🎯 監査基準
2026年Google検索アルゴリズム・スパムポリシー完全準拠

## ❌ 検出された重大な問題

### 1. noindex設定の欠如（最重要）

**現状**
- タグページ、日付アーカイブ、著者アーカイブに対するnoindex設定が未実装
- ページネーション2ページ目以降がindexされる可能性
- 検索結果ページがindexされる可能性

**リスク**
- 低品質ページのindex → サイト全体の評価低下
- 重複コンテンツ問題
- クロールバジェットの無駄遣い

**対応必須度**: 🔴 最優先

---

### 2. カテゴリページがSEOハブとして機能していない

**現状**
- `archive.php`は記事一覧のみを表示
- カテゴリの導入文・要点整理・内部リンク戦略が不在
- 検索意図を満たすコンテンツがない

**リスク**
- カテゴリページがSEO評価を得られない
- 内部リンク構造が弱い
- ユーザーのナビゲーション体験が悪い

**対応必須度**: 🔴 最優先

---

### 3. 構造化データの不足

**現状**
- Article構造化データが未実装
- Breadcrumb構造化データが未実装
- 検索エンジンがコンテンツ構造を正しく理解できない

**リスク**
- リッチリザルト表示の機会損失
- CTR低下

**対応必須度**: 🟡 高優先

---

### 4. 内部リンク構造の問題

**現状**
- 記事→記事の横断リンクが主体と推測
- カテゴリを経由した階層構造が弱い
- アンカーテキストの最適化が不明

**リスク**
- SEO評価の分散
- クロール効率の低下

**対応必須度**: 🟡 高優先

---

### 5. ページネーションのSEO対策不足

**現状**
- 2ページ目以降のnoindex設定が未確認
- rel="prev/next"の実装状況が不明

**リスク**
- 重複コンテンツ問題
- 評価の分散

**対応必須度**: 🟡 高優先

---

## ✅ 良好な点

1. **テンプレート構造**: 基本的なWordPressテンプレート階層に準拠
2. **デザイン**: 黄色ベースの統一されたトンマナ
3. **レスポンシブ**: モバイル対応済み
4. **メタ情報**: 日付・カテゴリーの表示あり

---

## 🔧 必須対応事項（優先順位順）

### 【最優先】1. noindex設定の実装

**対応内容**
```php
// functions.phpに追加
add_action('wp_head', 'gachasoku_seo_noindex_rules', 1);
function gachasoku_seo_noindex_rules() {
  // タグページ
  if (is_tag()) {
    echo '<meta name="robots" content="noindex, follow">' . "\n";
  }
  
  // 日付アーカイブ
  if (is_date()) {
    echo '<meta name="robots" content="noindex, follow">' . "\n";
  }
  
  // 著者アーカイブ
  if (is_author()) {
    echo '<meta name="robots" content="noindex, follow">' . "\n";
  }
  
  // 検索結果
  if (is_search()) {
    echo '<meta name="robots" content="noindex, follow">' . "\n";
  }
  
  // ページネーション2ページ目以降
  if (is_paged() && get_query_var('paged') > 1) {
    echo '<meta name="robots" content="noindex, follow">' . "\n";
  }
  
  // 絞り込み・並び替えURL
  if (isset($_GET['site']) || isset($_GET['orderby'])) {
    echo '<meta name="robots" content="noindex, follow">' . "\n";
  }
}
```

---

### 【最優先】2. カテゴリページのSEOハブ化

**対応内容**

#### A. category.phpの新規作成
- archive.phpとは別にカテゴリ専用テンプレートを作成
- カテゴリ説明文（導入文）の表示
- 主要記事へのピックアップリンク
- カテゴリ全体の要点整理セクション

#### B. カテゴリ説明文の管理UI
- WordPress管理画面でカテゴリごとに説明文を設定可能に
- SEO的な導入文を記述できる仕組み

#### C. 内部リンク戦略
- カテゴリページから主要記事（エース記事）への明示的リンク
- 初心者向け・比較・注意点など役割別の記事分類表示

---

### 【高優先】3. 構造化データの実装

**対応内容**

#### A. Article構造化データ（single.php）
```php
// 記事ページにJSON-LD形式で追加
{
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": "記事タイトル",
  "datePublished": "公開日",
  "dateModified": "更新日",
  "author": {...},
  "publisher": {...},
  "image": "アイキャッチ画像"
}
```

#### B. Breadcrumb構造化データ
```php
// 全ページに階層構造を明示
トップ > カテゴリ > 記事
```

---

### 【高優先】4. 内部リンク構造の最適化

**対応内容**

#### A. single.phpの改善
- 記事下部に「このカテゴリの他の記事」セクション追加
- カテゴリページへの明示的リンク
- 関連記事は同カテゴリ内から優先表示

#### B. アンカーテキストの最適化
- 「こちら」「詳しくはこちら」の禁止
- 内容が分かる具体的な文言を使用

---

### 【中優先】5. ページネーションの最適化

**対応内容**
- 2ページ目以降はnoindex設定（上記1で対応）
- rel="prev/next"の実装（WordPress標準で対応済みの場合が多い）

---

## 📊 実装後の期待効果

1. **クロール効率の向上**: 低品質ページのnoindexにより、重要ページへのクロール集中
2. **評価の集中**: カテゴリページがSEOハブとして機能し、評価が分散しない
3. **CTR向上**: 構造化データによるリッチリザルト表示
4. **ユーザー体験向上**: カテゴリページでの情報整理とナビゲーション改善
5. **長期的な評価積み上げ**: スパムポリシー準拠による安定した評価

---

## ⚠️ 実装時の注意事項

1. **既存記事への影響**: noindex設定は慎重に（カテゴリページは基本的にindex）
2. **カテゴリ説明文**: 各カテゴリに質の高い導入文を用意する必要あり
3. **段階的実装**: 一度にすべて変更せず、効果を確認しながら進める
4. **Search Consoleでの確認**: 実装後、indexステータスを定期的に確認

---

## 🎯 次のステップ

1. ✅ このレポートの確認と承認
2. 🔧 noindex設定の実装（functions.php）
3. 🔧 category.phpの作成とSEOハブ化
4. 🔧 構造化データの実装
5. 🔧 内部リンク構造の改善
6. 📊 実装後のモニタリング（Search Console）

---

## 📝 補足：2026年SEO指示書との対応

| 指示書の項目 | 現状 | 対応 |
|---|---|---|
| タグページnoindex | ❌ 未実装 | ✅ 実装予定 |
| 日付アーカイブnoindex | ❌ 未実装 | ✅ 実装予定 |
| 著者アーカイブnoindex | ❌ 未実装 | ✅ 実装予定 |
| ページネーション2ページ目以降noindex | ❌ 未実装 | ✅ 実装予定 |
| カテゴリ=SEOハブ | ❌ 記事一覧のみ | ✅ 実装予定 |
| 構造化データ | ❌ 未実装 | ✅ 実装予定 |
| 内部リンク階層構造 | ⚠️ 要改善 | ✅ 実装予定 |

---

**監査者**: Manus AI（2026年版SEO指示書準拠）
